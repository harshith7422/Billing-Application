exports.getUsageDetails = async (userId) => {
    // Fetch usage details from the database or external service
    return {
        userId,
        usage: {
            data: 'example usage data'
        }
    };
};
